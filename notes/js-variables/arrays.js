let array = [];

// array.push(valor) = adiciona um valor ao final do array

// array.pop(valor) = remove o valor do final do array

// array.unshift(valor) = adiciona o valor ao início do array

// array.shift() = remove o primeiro valor do array

// array.includes(valor) = verifica se o valor existe dentro do meu array

// array.every(item => item === 5) = verifica se todos os itens são iguais ao 5

// array.some(item => item === 5) = verifica se algum dos itens é igual a 5

// array.reverse() = inverte a posição dos valores